# خطة تطوير التصميم و UI/UX — لوحة مزوّد الخدمة (car-hero-provider-dashboard)

> النطاق: **التصميم وتجربة المستخدم فقط**. لا تغييرات في الـ API أو منطق الأعمال أو الخادم.
> أي بند يمسّ البيانات مذكور فقط لأن له أثراً بصرياً مباشراً (تنسيق الأرقام/التواريخ، حالة الاتصال المعروضة).
>
> الحزمة الحالية: Next.js 16 (App Router) + React 19 + Tailwind v4 + `@base-ui/react` + ECharts + sonner.

---

## 0) خلاصة التشخيص (ما وجدته فعلياً في الكود)

| # | المشكلة | الدليل |
|---|---------|--------|
| 1 | `<html lang="en">` بدون `dir="rtl"` — الاتجاه مفروض من CSS فقط (`html { direction: rtl }`) | `src/app/layout.tsx:28`, `globals.css:88` |
| 2 | الخط العربي **غير محمّل إطلاقاً**: الـ CSS يطلب `IBM Plex Sans Arabic` بينما `next/font` يحمّل `Geist` اللاتيني فقط → النص العربي يسقط على خط النظام | `layout.tsx:6-14` مقابل `globals.css:92` |
| 3 | **27 استخدام لـ `dark:`** في مكوّنات `ui/` بينما لا يوجد `.dark` على أي عنصر → كل تلك الأنماط ميتة، ومن هنا وُلد الالتفاف `.dark-input` بـ `!important` | `grep dark:` = 27، لا `class="dark"` في المشروع |
| 4 | **0 استخدام لخصائص المسافات المنطقية** (`ms-/me-/ps-/pe-`) مقابل 28 استخداماً فيزيائياً (`mr-/ml-/pr-/pl-`) في واجهة RTL | مثال: `mr-1.5` على أيقونات أزرار `booking-card.tsx` |
| 5 | `Card` الأساسي ليس `relative`، لكن **21 موضعاً** يضع داخله `<div className="absolute inset-0">` لتوهّج الـ hover → الطبقة تهرب خارج البطاقة | `ui/card.tsx` مقابل `booking-card.tsx`, `overview-*.tsx` |
| 6 | `Card` يفرض `ring-1 ring-foreground/10`، وكل صفحة تضيف `border border-border/20` → حافة مزدوجة | `ui/card.tsx:16` + كل الصفحات |
| 7 | فوضى الأنصاف: 16 قيمة زوايا مختلفة (`rounded-xl` ×49، `rounded-lg` ×41، `rounded-[1.5rem]` ×13، `rounded-[1.25rem]` ×7 …) | `grep rounded-` |
| 8 | فوضى المقاييس النصية: 63 حجماً عشوائياً (`text-[11px]` ×24، `text-[10px]` ×12، `text-[9px]` ×2 …) — صغيرة جداً على العربية | `grep text-\[..px\]` |
| 9 | تنسيق الأرقام غير موحّد: `ar-SY` ×11، `ar-EG` ×3، `ar-SA` ×2، وبدون locale ×3 → أرقام هندية ولاتينية في الشاشة نفسها | `grep toLocaleString` |
| 10 | تاريخ الهيدر بـ `toLocaleDateString("ar-SA")` → **تقويم هجري** بينما بقية النظام ميلادي | `components/layout/header.tsx:36` |
| 11 | العملة: المحفظة تعرض `wallet.currency` (افتراضه `SAR`) بينما بقية الشاشات تكتب `ل.س` يدوياً | `finance/page.tsx:99` مقابل `booking-card.tsx` |
| 12 | عنوان الصفحة مكرّر مرتين: مرة في `Header` ومرة داخل الصفحة نفسها (الطلبات، المالية، الخدمات…) | `header.tsx` + `orders/page.tsx:210` |
| 13 | عرض المحتوى يقفز بين المسارات: بلا حد أقصى (الرئيسية) / `max-w-7xl` / `max-w-4xl` / `max-w-3xl` | مقارنة صفحات `(dashboard)` |
| 14 | شارة **Live** في الشريط الجانبي ثابتة ولا تعكس حالة السوكِت الحقيقية، وجرس الإشعارات `notifCount = 0 // TODO` بلا قائمة | `sidebar.tsx:83`, `header.tsx:31` |
| 15 | `Header` يستقبل `onRefresh` لكن `layout.tsx` لا يمرّره أبداً → زر تحديث ميت في القشرة، وكل صفحة صنعت زرّها الخاص | `layout.tsx:60` |
| 16 | زر قائمة الجوال `fixed top-3.5 right-3.5 z-[60]` يغطي هوية الصفحة في الهيدر على الشاشات الصغيرة | `sidebar.tsx:230` |
| 17 | درج الجوال مكتوب يدوياً: بلا `aria-modal`، بلا حبس تركيز، بلا `aria-expanded`، وبلا `aria-current` على الرابط النشط | `sidebar.tsx` |
| 18 | التبويبات: `role="tab"` بلا `role="tablist"` صحيح في الطلبات، وبلا `tabpanel`/`aria-controls`/تنقّل بالأسهم | `orders/page.tsx:229`, `settings/page.tsx:88` |
| 19 | لا يوجد `prefers-reduced-motion` مطلقاً رغم 6 حركات لا نهائية (ping/pulse/shine/float/border-glow) | `grep reduced-motion` = 0 |
| 20 | 35 استخدام `backdrop-blur` متداخلة (كثير منها `blur-2xl`) → كلفة رسم عالية على الأجهزة المتوسطة | `grep backdrop-blur` |
| 21 | نصوص بشفافية `/40` و`/50` فوق خلفية داكنة جداً → تباين أقل من WCAG AA بكثير | `text-muted-foreground/40` منتشر |
| 22 | `text-white` مكتوب يدوياً 20 مرة بدل `text-foreground` | `grep text-white` |
| 23 | الرسوم البيانية بألوان hex مثبّتة (`#a57ed8`, `#10b981`…) لا تتبع التوكنات، و`--chart-1..5` معرّفة وغير مستخدمة | `overview-*.tsx`, `globals.css:69` |
| 24 | `<select>` أصلية غير منسّقة (6 منها في صف الفلاتر) — قائمة الخيارات تظهر بألوان النظام لأن `color-scheme` غير مضبوط | `orders/page.tsx`, `finance/page.tsx` |
| 25 | حالات التحميل/الخطأ/الفراغ مختلفة في كل صفحة (سبينر / نبض / هيكل عظمي / نص) | مقارنة `services` و`orders` و`finance` |
| 26 | لا `loading.tsx` ولا `error.tsx` ولا `not-found.tsx`، وكل الصفحات `"use client"` → عنوان التبويب ثابت لكل المسارات | `app/` |
| 27 | `Toaster position="top-right"` في واجهة RTL | `providers.tsx:30` |
| 28 | مؤشرات StatCard مضلّلة: عدّاد "بانتظار الإجراء" يظهر بسهم أخضر صاعد، ونص "نقاط بيانات الإيراد" غير مفهوم للمستخدم | `overview-stats-cards.tsx:52-66` |
| 29 | أيقونات الصفحات إيموجي (🏠 📋 🔧 💰) داخل لوحة يُفترض أنها احترافية | `header.tsx:12-19` |
| 30 | `main` يخفي التجاوز الأفقي بـ `overflow-x-hidden` + `max-w-[100vw]` بدل معالجة سببه | `(dashboard)/layout.tsx:64` |

---

## المرحلة 1 — الأساسات: اللغة والاتجاه والخط والتوكنات ✅ **مُنجزة**

**الهدف:** تصحيح الجذور التي تُفسد كل ما فوقها. تغييرات صغيرة الحجم، أثرها في كل بكسل.

> **حالة التنفيذ (2026-08-18):** مُنجزة بالكامل ومُتحقَّق منها في المتصفح.
> `tsc` و`eslint` و`next build` و`vitest` (8 اختبارات) كلها ناجحة.
> تفاصيل ما نُفِّذ وما تبقّى مقصوداً في قسم "نتائج المرحلة 1" أسفل الملف.

### 1.1 اللغة والاتجاه
- `<html lang="ar" dir="rtl">` في `app/layout.tsx`، وحذف `direction: rtl` من `globals.css` (تكرار).
- إزالة `dir="rtl"` اليدوية الـ18 من الحوارات والبطاقات، والإبقاء على `dir="ltr"` **فقط** لما يستحق: أرقام الهواتف، IBAN، رقم الطلب، البريد الإلكتروني.
- استبدال كل `mr-*/ml-*/pr-*/pl-*` بـ `me-*/ms-*/pe-*/ps-*` (28 موضعاً)، أو الأفضل: استبدالها بـ `gap` على الحاوية.
- مراجعة اتجاه الأيقونات: أسهم التنقّل والتالي/السابق والـ chevrons.

### 1.2 الطباعة (Typography)
- تحميل خط عربي حقيقي عبر `next/font/google` — `IBM_Plex_Sans_Arabic` (أوزان 400/500/600/700) وربطه بمتغيّر `--font-arabic`.
- تسجيله في `@theme` كـ `--font-sans` ليعمل `font-sans` في Tailwind، وحذف سلسلة `font-family` اليدوية من `body`.
- ملاحظة: العربية تحتاج `line-height` أعلى (1.7–1.8 للنص، 1.3 للعناوين) و`letter-spacing: 0` — إزالة `tracking-tight`/`tracking-wide`/`tracking-[0.2em]` من النصوص العربية (تُفسد اتصال الحروف)، والاحتفاظ بها للنصوص اللاتينية فقط.

### 1.3 نظام التوكنات
- تفعيل الوضع الداكن بشكل صريح: `<html class="dark">` أو تحويل الـ 27 `dark:` إلى أنماط أساسية — واحد من الاثنين، لا ثالث لهما. النتيجة: حذف `.dark-input` و`!important` نهائياً.
- `color-scheme: dark` على `:root` → إصلاح `<select>`/`<input type="date">`/`<input type="time">` وأشرطة التمرير أصلياً.
- توحيد التوكنات في `globals.css`:
  - **سلّم زوايا** من 4 قيم (`sm/md/lg/xl`) ونقل كل `rounded-[1.25rem]`/`[1.5rem]` إليها.
  - **سلّم ظلال** من 3 مستويات (`elev-1/2/3`) بدل `shadow-2xl shadow-black/20` المكرّر + التوهّجات الخمسة.
  - **ألوان دلالية**: `--success/--warning/--danger/--info` بدل استخدام `emerald/amber/rose/sky/violet/cyan/indigo` الخام المتناثرة.
  - تفعيل `--chart-1..5` واستخدامها في كل الرسوم.
- `prefers-reduced-motion: reduce` → تعطيل كل الحركات اللانهائية والانتقالات.

**معيار القبول:** بناء ناجح، صفر `!important` في `globals.css`، صفر `dark:` معطّل، النص العربي يُعرض بخط IBM Plex Sans Arabic (مؤكَّد من DevTools)، ولا اختلاف بصري غير مقصود بين قبل/بعد.

---

## المرحلة 2 — نظام التصميم: المكوّنات الأساسية ✅ **مُنجزة**

**الهدف:** مكتبة مكوّنات واحدة يُبنى عليها كل شيء، بدل 27 بطاقة كل واحدة بأنماطها.

> **حالة التنفيذ (2026-08-18):** مُنجزة، والتوثيق في `DESIGN_SYSTEM.md`.
> تفاصيل ما نُفِّذ وما تبقّى مقصوداً في قسم "نتائج المرحلة 2" أسفل الملف.

### 2.1 إصلاح البدائيات (`src/components/ui/`)
- **`Card`**: إضافة `relative` (تُصلح 21 طبقة توهّج هاربة)، حذف `ring-1` المفروض أو حذف الحدود المضافة في الصفحات — لا الاثنان معاً، وتوحيد الحشو عبر `size` بدل `p-5/p-6/p-7` العشوائية.
- **`Button`**: مراجعة المقاسات (`sm` بارتفاع 28px صغير على اللمس)، ضمان **44×44 كحد أدنى لأهداف اللمس**، إضافة حالة `loading` مدمجة (بدل تكرار `<Loader2 className="animate-spin" />` في 9 مواضع)، وتقوية `destructive` للأفعال الخطرة.
- **`Input` / `Textarea` / `Label`**: حالات موحّدة (default / hover / focus / error / disabled)، ورسالة خطأ أسفل الحقل بدل الاعتماد على الـ toast فقط.
- **`Badge`**: توحيدها مع `StatusBadge` و`badge-*` في CSS — ثلاث آليات لنفس الغرض حالياً.
- **`Skeleton`**: نسخة واحدة (`animate-pulse` في `ui/skeleton` مقابل `.skeleton` shimmer في CSS مقابل `h-64 animate-pulse` يدوي).

### 2.2 مكوّنات جديدة مشتركة
| المكوّن | يستبدل |
|---------|--------|
| `<Select>` (مبني على `@base-ui/react`) | 11 `<select>` أصلية غير منسّقة |
| `<PageHeader>` | 6 رؤوس صفحات مكتوبة يدوياً |
| `<EmptyState>` | 6 حالات فراغ مختلفة الشكل |
| `<ErrorState>` | 5 حالات خطأ مختلفة |
| `<DataToolbar>` (بحث + فلاتر + رقائق فلاتر نشطة + مسح) | شبكة الفلاتر المكرّرة في الطلبات والمالية |
| `<Pagination>` | زرّا "التالي/السابق" المكرّران |
| `<ConfirmDialog>` | 4 حوارات تأكيد بترتيب أزرار مختلف |
| `<Money>` / `<DateText>` | 19 استدعاء `toLocaleString` بـ4 لغات مختلفة |

### 2.3 التغذية الراجعة
- `Toaster` إلى `top-center` أو `top-left` (RTL)، مع مدد موحّدة ونصوص موحّدة النبرة.
- سياسة واضحة: **Toast** للنتائج العابرة، **Inline** لأخطاء الحقول، **Banner** لحالات الحساب، **Dialog** للقرارات الحاجزة فقط.

**معيار القبول:** ملف `DESIGN_SYSTEM.md` يوثّق التوكنات والمكوّنات؛ أي صفحة جديدة تُبنى دون كتابة CSS مخصّص.

---

## المرحلة 3 — القشرة والتنقّل (App Shell & IA) ✅ **مُنجزة**

**الهدف:** إطار ثابت يمنح المزوّد إحساساً بمكانه وبما يحتاج فعله الآن.

> **حالة التنفيذ (2026-08-18):** مُنجزة ومُتحقَّق منها على بناء الإنتاج.
> تفاصيلها في قسم "نتائج المرحلة 3" أسفل الملف.

### 3.1 الهيدر
- إزالة تكرار العنوان: العنوان في **الهيدر فقط**، والصفحة تبدأ مباشرة بالمحتوى (أو العكس) — يوفّر ~120px من الطية الأولى في كل صفحة.
- استبدال الإيموجي بأيقونات Lucide متسقة مع الشريط الجانبي.
- تاريخ ميلادي بلغة عربية (`ar` + `gregory`) بدل الهجري.
- **زر تحديث واحد** في الهيدر يعمل فعلاً (تمرير `onRefresh` من التخطيط أو عبر context)، وحذف الأزرار المكرّرة من الصفحات.
- **مركز إشعارات حقيقي**: قائمة منسدلة بآخر الأحداث + عدّاد مرتبط بمصدر بيانات فعلي، بدل `notifCount = 0`.
- **مؤشر اتصال صادق**: شارة Live تعكس حالة السوكِت (متصل / يعيد الاتصال / منقطع) — المؤشر الكاذب أسوأ من غيابه.

### 3.2 الشريط الجانبي
- طيّ/بسط على الديسكتوب (أيقونات فقط + tooltip) مع حفظ التفضيل.
- `aria-current="page"` على الرابط النشط، و`<nav aria-label>` واضح.
- درج الجوال: `role="dialog"` + `aria-modal` + حبس التركيز + إعادة التركيز عند الإغلاق + `aria-expanded` على الزر.
- نقل زر القائمة إلى داخل الهيدر (يزول تعارض `z-[60]` مع عنوان الصفحة).
- بطاقة المزوّد أسفل الشريط: إضافة **مبدّل حالة التوفّر (متاح/مشغول/غير متصل)** — عنصر تحكّم يومي مفقود تماماً من الواجهة رغم أن ظهور المزوّد للعملاء يعتمد عليه.

### 3.3 التخطيط
- عرض محتوى موحّد (`max-w-[1440px]` مع حشو متجاوب) لكل المسارات → لا قفزات عند التنقّل.
- إيقاع رأسي موحّد (`space-y-6`) بدل `space-y-10 / 7 / 6 / 5`.
- سلّم z-index موثّق (`--z-header/-drawer/-overlay/-toast`) بدل `z-30/50/55/58/60` العشوائية.
- رابط "تخطّي إلى المحتوى" + معالم HTML صحيحة.
- إزالة `overflow-x-hidden` بعد إصلاح مصادر التجاوز الفعلية.

**معيار القبول:** التنقّل بين المسارات الستة بلا أي قفزة تخطيط؛ الشريط الجانبي قابل للتشغيل بالكيبورد بالكامل؛ لا عنصر واجهة يعرض حالة غير حقيقية.

---

## المرحلة 4 — الشاشات: إعادة تصميم بحسب المهمّة

**الهدف:** كل شاشة تجيب عن سؤال المزوّد الأول خلال 3 ثوانٍ.

### 4.1 الرئيسية (`/`)
- الطية الأولى تجيب: **"ما الذي يحتاج تدخّلي الآن؟"** — الطلبات المعلّقة أولاً، ثم الأرقام.
- بطاقة الترحيب الضخمة (176px من الارتفاع لجملة ترحيب) تتقلّص إلى شريط حالة مضغوط.
- إصلاح دلالة المؤشرات: لا سهم أخضر صاعد لعدّاد "بانتظار الإجراء"؛ ومقارنة حقيقية بالفترة السابقة أو لا مؤشر إطلاقاً.
- إزالة الـ sparkline الوهمية (مصفوفات فارغة لبطاقتي التقييم والخدمات).
- الرادار السداسي بديله شريط أفقي مرتّب (أسهل قراءة لمقارنة الخدمات، وقابل للقراءة على الجوال).
- تنبيهات النظام: تظهر **فقط عند وجود إجراء**، وبأزرار تنفيذ (مثل: "أضف خدماتك ←")، لا نصوص عامة دائمة.

### 4.2 الطلبات (`/orders`)
- **مبدّل عرض: بطاقات ↔ جدول.** الجدول (رأس ثابت، أعمدة قابلة للفرز) هو الأنسب لأكثر من 20 صفاً.
- شريط أدوات موحّد: بحث + رقائق فلاتر نشطة قابلة للإزالة + عدد النتائج + مسح الكل.
- ترقيم صفحات كامل (أرقام + قفز + اختيار حجم الصفحة) بدل التالي/السابق فقط.
- تسلسل بصري للإجراء داخل البطاقة: إجراء أساسي واحد بارز، والباقي ثانوي — حالياً ثلاثة أزرار متدرّجة الألوان تتنافس بصرياً.
- لوحة تفاصيل جانبية (Sheet) بدل حوار ضيّق `max-w-lg` لبيانات كثيرة.
- الرسم الأسبوعي فوق القائمة يُنقل أو يُطوى (يدفع القائمة تحت الطية).

### 4.3 المالية (`/finance`)
- توحيد العملة والفواصل العشرية في مكان واحد.
- سجل المعاملات كجدول حقيقي مع رأس أعمدة (الشبكة الحالية بلا رؤوس تُقرأ بصعوبة)، وتصميم مكدّس واضح على الجوال.
- حالة "طلب سحب" مرئية كخط زمني (مُقدَّم → قيد المراجعة → مُحوَّل) بدل شارة نصية.
- حوار السحب: عرض الرصيد المتاح والحد الأدنى **قبل** الإدخال، تحقّق فوري داخل الحقل، وزر معطّل بسبب مشروح.

### 4.4 الخدمات (`/services`)
- تقسيم الشاشة إلى **"خدماتي"** و**"أضف من الكتالوج"** — دمجهما حالياً يخفي ما هو مفعَّل فعلاً.
- تحرير السعر مباشرة في البطاقة (inline) بدل حوار لكل تعديل.
- تجميع الكتالوج حسب الفئة مع رؤوس، وحفظ متفائل مع إمكانية التراجع.
- إصلاح كثافة الكود في `service-card.tsx`/`services/page.tsx` (أسطر بطول 400+ حرف) لأنها تعوق أي تحسين لاحق.

### 4.5 أوقات الدوام (`/working-hours`)
- معاينة أسبوعية بصرية (شريط زمني) تلخّص الجدول فوق الصفوف.
- قوالب سريعة: "كل الأيام 8–18"، "عطلة الجمعة"، "24/7".
- دعم فترتين في اليوم (صباحية/مسائية) إن كان النموذج يسمح، وإلا توضيح القيد نصياً.
- شريط حفظ ثابت أسفل الشاشة عند وجود تعديلات غير محفوظة + تحذير عند مغادرة الصفحة.

### 4.6 الإعدادات (`/settings`)
- التبويبات كمسارات فعلية (`/settings/profile` …) لتكون قابلة للمشاركة والرجوع بالزر الخلفي.
- رافع الوثائق: معاينة مصغّرة، شريط تقدّم داخل العنصر، رفع متعدد، وحالة مراجعة لكل مستند لا للحساب ككل.
- التحقق داخل الحقول لحظياً بدل toast بعد الإرسال.

### 4.7 تسجيل الدخول (`/login`)
- تسمية الحقل صحيحة (`id="email"` لحقل هاتف حالياً) + `autoComplete="tel"` و`inputMode`.
- خطأ الدخول يظهر داخل النموذج، لا في toast يختفي.
- `+963` كبادئة ثابتة مرئية (كما في تطبيق العميل) بدل توقّع كتابتها.
- المسافات الضخمة (`mt-16 mb-10`) تُضبط، والبطاقة تُختصر لتظهر كاملة على شاشة 1366×768.

### 4.8 عرضياً على كل الشاشات
- توحيد تنسيق الأرقام والتواريخ والعملة عبر `<Money>`/`<DateText>` (بند 2.2).
- توحيد ألوان الرسوم على `--chart-*`، مع بديل نصي/جدولي للرسم لقارئات الشاشة.
- توحيد حالات التحميل: هيكل عظمي يطابق شكل المحتوى النهائي، لا سبينر في المنتصف.

**معيار القبول:** لكل شاشة: حالة تحميل، حالة فراغ، حالة خطأ، وحالة بيانات كثيفة (100+ صف) — الأربعة مختبَرة بصرياً.

---

## المرحلة 5 — الصقل والجودة: وصول، أداء، تجاوب

**الهدف:** أن تصمد اللوحة أمام الاستخدام اليومي الحقيقي وأمام التدقيق.

### 5.1 إمكانية الوصول (a11y)
- تدقيق تباين كامل: كل نص يحقّق **4.5:1** (و3:1 للنص الكبير) — يعني إزالة `text-muted-foreground/40` و`/50` المنتشرة.
- تشغيل كامل بالكيبورد: التبويبات بالأسهم، الحوارات بحبس تركيز، القوائم بـ Escape، ترتيب Tab منطقي في RTL.
- حلقة تركيز مرئية على كل عنصر تفاعلي (موجودة عالمياً لكن تُغطّى بالتحويلات والظلال في عدة مواضع).
- تسميات ARIA للأيقونات المجرّدة، و`aria-live` للعدّادات المتغيّرة والإشعارات الفورية.
- تدقيق بقارئ شاشة على المسارات الستة.

### 5.2 الأداء البصري
- تقليص `backdrop-blur` من 35 موضعاً إلى الأسطح الطافية فقط (الشريط الجانبي، الهيدر، الحوارات) — الطبقات المتداخلة أغلى بكثير مما تبدو.
- تقليل التحريكات اللانهائية إلى مؤشرات الحالة الحقيقية فقط.
- تحميل ECharts بشكل كسول مع حجز ارتفاع ثابت (منع CLS)، واستيراد وحدات ECharts المطلوبة فقط بدل الحزمة الكاملة.
- قياس: **CLS < 0.1**، **LCP < 2.5s** على المسارات الستة.

### 5.3 التجاوب
- مصفوفة اختبار: **360 / 414 / 768 / 1024 / 1280 / 1440 / 1920**.
- لا تمرير أفقي في أي عرض (بعد إزالة `overflow-x-hidden`).
- تحويل كل جدول/شبكة إلى بطاقات مكدّسة تحت 768px مع تسميات ظاهرة.
- أهداف لمس ≥ 44×44 (عدة أزرار `size="sm"` بارتفاع 28px حالياً).

### 5.4 اكتمال المنصّة
- `loading.tsx` و`error.tsx` و`not-found.tsx` لمجموعة `(dashboard)`.
- عنوان تبويب لكل مسار (فصل الغلاف الخادمي عن مكوّن العميل ليعمل `metadata`).
- `favicon` وبيانات وصفية بالعربية.
- أنماط طباعة لتقارير المالية.

### 5.5 التوثيق والاستدامة
- `DESIGN_SYSTEM.md`: التوكنات، السلالم، المكوّنات، قواعد RTL، أمثلة استخدام.
- `UI_CHECKLIST.md`: قائمة مراجعة بصرية قبل أي دمج.
- لقطات مرجعية (before/after) لكل شاشة.

**معيار القبول:** صفر انتهاك تباين، صفر تمرير أفقي، تشغيل كامل بالكيبورد، وتوثيق يمكّن أي مطوّر من إضافة شاشة متسقة دون سؤال.

---

## ترتيب التنفيذ والاعتماديات

```
المرحلة 1 (أساسات)  ──▶ المرحلة 2 (مكوّنات) ──▶ المرحلة 3 (قشرة) ──▶ المرحلة 4 (شاشات) ──▶ المرحلة 5 (صقل)
     │                        │                                              ▲
     └── تصحّح الجذور          └── كل ما بعدها يُبنى عليها ───────────────────┘
```

الترتيب **إلزامي**: تحسين الشاشات قبل توحيد التوكنات يعني إعادة كتابة نفس الشاشات مرتين.

## خارج النطاق (بشكل صريح)
تغييرات الـ API، منطق الأعمال، المصادقة، الأداء الخادمي، الاختبارات الوظيفية، وأي تعديل في `CAR_HERO_BACKEND`.

---

# نتائج المرحلة 1 — سجل التنفيذ

## ما تغيّر فعلياً

### `src/app/layout.tsx`
- `lang="ar" dir="rtl"` بدل `lang="en"` بلا اتجاه.
- `class="dark"` على `<html>` → أحيا **27 نمط `dark:`** كانت ميتة في بدائيات `ui/`.
- استبدال `Geist`/`Geist_Mono` بـ **`IBM_Plex_Sans_Arabic`** (400/500/600/700، مجموعتا `arabic`+`latin`) و**`IBM_Plex_Mono`** للمعرّفات اللاتينية.
- بيانات وصفية عربية مع `title.template`.

### `src/app/globals.css`
- حذف `direction: rtl` (صار من سمة `dir`).
- `color-scheme: dark` على `:root` → قوائم `select` ومنتقيات `date`/`time` وأشرطة التمرير داكنة **أصلاً**، فسقطت الحاجة إلى `.dark-input`.
- **حذف `.dark-input` بالكامل** ومعها قاعدة `input:focus, textarea:focus, select:focus { …!important }`.
- إضافة `.field`: نمط واحد لعناصر التحكّم الأصلية يطابق `<Input>` تماماً، بلا `!important`.
- **إصلاح خطأ حقيقي:** `:focus-visible` كان يعيّن `border-radius: var(--radius)` — أي أن زر `rounded-full` كان يتحوّل إلى 12px لحظة التركيز. حُذفت الخاصية.
- توكنات جديدة: ألوان دلالية (`--success/--warning/--danger/--info` + درجات `-soft`)، سلّم زوايا (`--radius-sm..3xl`)، سلّم ارتفاعات (`--shadow-elev-1..3`)، سلّم طبقات (`--z-header..overlay`).
- إعادة ربط `.glow-*` و`.badge-*` و`.gradient-bg` و`live-pulse` بالتوكنات بدل قيم HSL مثبّتة.
- `--font-weight-black: 700` — العائلة لا تتجاوز 700، والربط يمنع التغميق الصناعي الذي يشوّه اتصال الحروف العربية.
- ارتفاع سطر 1.75 للنص و1.35 للعناوين، و`letter-spacing: 0`.
- كتلة `@media (prefers-reduced-motion: reduce)` تُلغي الزخرفة اللانهائية وتُبقي `animate-spin` (مؤشر حالة، لا زخرفة).
- `.stat-blob` و`.sidebar-item-active::before` إلى `inset-inline-*`.

### الشاشات
- **11 عنصر `<select>`** انتقلت من `h-9 rounded-lg border border-input dark-input px-3 …` إلى `field h-9 px-3 text-sm`.
- **كل المسافات الفيزيائية → منطقية**: `mr/ml/pr/pl` ← `me/ms/pe/ps` (الرصيد الآن **صفر**).
- **كل المواضع الفيزيائية → منطقية**: `right-*/left-*` ← `start-*/end-*` في الشريط الجانبي وأيقونات البحث وزر إظهار كلمة المرور وشارة الإشعارات ونقطة التنبيه وزر إغلاق الحوار.
- `mr-1.5` ×7 على أيقونات أزرار `booking-card` كانت تضع الفراغ على الجانب الخطأ في RTL — حُذفت اعتماداً على `gap` المدمج في `Button`.
- **إزالة `tracking-*` و`uppercase` عن كل نص عربي** (كانا يقطعان اتصال الحروف)؛ بقيا فقط على المحتوى اللاتيني الموسوم `dir="ltr"` وشارة `Live`.
- إزالة **9 سمات `dir="rtl"`** صارت بلا عمل داخل مستند RTL، مع الإبقاء على `dir="ltr"` لأرقام الطلبات والمعاملات والهاتف والبريد و IBAN.
- `Toaster` من `top-right` إلى `top-left` (المقابل الصحيح في RTL).

### `.claude/launch.json`
- تهيئة `provider-dashboard` على المنفذ 3005 (تفادياً لتضارب 3002 مع `admin`).

## التحقّق

| الفحص | النتيجة |
|-------|---------|
| `npx tsc --noEmit` | ✅ بلا أخطاء |
| `npm run lint` | ✅ نظيف |
| `npm run build` | ✅ 8 مسارات مبنيّة |
| `npm test` | ✅ 8/8 |
| `html[lang]` / `html[dir]` | `ar` / `rtl` ✅ |
| `color-scheme` المحسوب | `dark` ✅ |
| الخط المحمَّل فعلياً | `IBM Plex Sans Arabic` بأوزان 400/500/600/700 ✅ |
| `letter-spacing` على body و h2 | `normal` ✅ |
| `font-black` المحسوب | `700` (لا تغميق صناعي) ✅ |
| أنماط `dark:` حيّة | زر `outline` صار بخلفية `input/30` بعد أن كان شفافاً ✅ |
| `.field` على `<select>` | خلفية `rgba(50,38,64,.3)` + حدّ + زاوية 10px + `color-scheme: dark` ✅ |
| أيقونة البحث ← `ps-10` | الأيقونة على الجانب الابتدائي والحشو يقابلها ✅ |
| مؤشّر التنقّل النشط | ملاصق للحافة الابتدائية (1px) ✅ |
| ثبات الزوايا عند التركيز | 16px قبل وبعد ✅ |
| كتلة `prefers-reduced-motion` | موجودة بأربع مجموعات قواعد ✅ |
| أخطاء الـ console | لا شيء عدا رفض اتصال الـ API (الخادم غير مُشغَّل) ✅ |

## ملاحظات صريحة

1. **`!important` لم يصل إلى صفر — بل إلى 7، كلها داخل كتلة `prefers-reduced-motion`.** هذا هو الاصطلاح الصحيح: كتلة تقليل الحركة يجب أن تتغلّب على utilities والأنماط السطرية وإلا فلا أثر لها. الـ `!important` الذي كان يمثّل مشكلة — وهو الذي كان يقاتل طبقة المكوّنات (`.dark-input` وقاعدة `input:focus`) — **حُذف بالكامل**.
2. **تفعيل `class="dark"` غيّر مظهر بعض العناصر بشكل مقصود**: حقول الإدخال وأزرار `outline` اكتسبت خلفية `input/30` بعد أن كانت شفافة تماماً. هذا هو المظهر الذي صُمّمت له بدائيات `@base-ui` أصلاً، وهو سبب وجود التفاف `.dark-input` من الأساس.
3. **زر إغلاق الحوار انتقل من أعلى-اليمين إلى أعلى-اليسار** — وهذا هو الانعكاس الصحيح في RTL (الجانب الانتهائي).
4. **`rounded-2xl` صار 20px بدل 16px** نتيجة توحيد سلّم الزوايا (دمج `rounded-2xl` مع `rounded-[1.25rem]`). فرق مقصود بمقدار 4px.
5. **`!h-[54px]` في صفحة الدخول لم يُلمَس** — إصلاحه يتبع إعادة تصميم مقاسات `Button`/`Input` في المرحلة 2.
6. **لم يُلتقط أي سكرين‑شوت**: لوحة المتصفح غير معروضة في هذه البيئة، فتعذّر التصوير. التحقّق تمّ عبر قياس الأنماط المحسوبة والصناديق في DOM مباشرة — وهو أدقّ من العين المجرّدة لهذه البنود تحديداً، لكنه لا يغني عن مراجعة بصرية بشرية.
7. **الخادم الخلفي غير مُشغَّل**، لذا اختُبرت القشرة والفلاتر وحالات الخطأ، لا الشاشات ببيانات حقيقية.

---

# نتائج المرحلة 2 — سجل التنفيذ

## ملفات جديدة

| الملف | الدور |
|-------|------|
| `src/lib/format.ts` | **مصدر التنسيق الوحيد**: `LOCALE = "ar-SY-u-nu-latn"` + دوال الأرقام والمبالغ والتواريخ والعملة |
| `src/components/ui/select.tsx` | قائمة منسدلة على `@base-ui` تستبدل 11 `<select>` أصلية |
| `src/components/ui/field.tsx` | تسمية + حقل + رسالة خطأ مربوطة بـ `aria-describedby`/`aria-invalid` |
| `src/components/ui/states.tsx` | `EmptyState` / `ErrorState` / `LoadingState` |
| `src/components/ui/page-header.tsx` | رأس الصفحة الموحّد |
| `src/components/ui/data-toolbar.tsx` | بحث + فلاتر + **رقائق الفلاتر النشطة** + مسح الكل |
| `src/components/ui/pagination.tsx` | ترقيم بأرقام صفحات ونافذة "…" |
| `src/components/ui/confirm-dialog.tsx` | حوار تأكيد واحد بترتيب أزرار ثابت |
| `src/components/ui/money.tsx` | `Money` و`RelativeTime` |
| `DESIGN_SYSTEM.md` | التوثيق + قائمة المراجعة قبل الدمج |

## بدائيات أُعيد بناؤها

- **`Card`**: أضيف `relative` (أصلح **21** طبقة توهّج كانت تهرب خارج البطاقة)، واستُبدل `ring-1` بحدّ واحد فأُزيلت الحافة المزدوجة من كل بطاقة.
- **`Button`**: خاصية `loading` (تستبدل نمط `{pending ? <Loader2/> : <Icon/>}` المكرّر في تسعة مواضع مع نسيان التعطيل في بعضها)، `destructive` مصمت + `destructive-soft`، حشو منطقي (`pe-`/`ps-` بدل `pr-`/`pl-` التي فاتت المرحلة 1 لوجودها داخل `has-data-[…]:`)، وتوسيع منطقة اللمس إلى 44px عبر `after` دون تغيير الحجم المرئي.
- **`Badge`**: متغيّرات دلالية `success/warning/danger/info/neutral`.
- **`Skeleton`** + `SkeletonList`: تنفيذ واحد بدل ثلاثة.
- **`StatCard`**: `tone` واحد بدل ثلاث سلاسل tailwind خام لكل بطاقة.
- **`Dialog`**: `max-h-[85vh]` مع تمرير داخلي — حوار تفاصيل الطلب كان يُقتطع على شاشات 768px.

## شاشات أُعيد ربطها

- **الطلبات**: `PageHeader` + `DataToolbar` برقائق + `Pagination` + `ConfirmDialog` + `EmptyState`/`ErrorState`. بطاقة الطلب صارت بـ**إجراء أساسي واحد بارز** بدل ثلاثة أزرار متدرّجة الألوان بالوزن نفسه.
- **المالية**: نفس المعاملة + شرح صريح لسبب تعطيل زرّ السحب بدل تركه ميتاً، وحوار السحب صار يعرض الرصيد والحد الأدنى **قبل** الإدخال مع تحقّق داخل الحقول.
- **الخدمات**: تحرير السعر **داخل البطاقة** (Enter للحفظ، Escape للإلغاء) بدل فتح حوار لتغيير رقم واحد.
- **أوقات الدوام**: `Switch` واحد لكل يوم بدل زرّ يبدّل لونه ونصّه، ورسالة الخطأ مربوطة بالحقل.
- **الإعدادات**: `Field` بتحقّق داخل الحقول (اسم/مدينة/بريد)، وحالة الوثائق من جدول بيانات بدل ثلاث نسخ JSX متكرّرة.
- **الرئيسية**: التنبيهات القابلة للتنفيذ **قبل** الأرقام، ولا تظهر إلا عند وجود إجراء فعلي؛ شريط ترحيب مضغوط بدل بطاقة 176px؛ ونصوص المؤشرات صارت صادقة (`—` للتقييم الغائب بدل `0.0`، ولا سهم أخضر صاعد لعدّاد الطلبات المعلّقة).
- **تسجيل الدخول**: الخطأ يبقى داخل النموذج، `id="phone"` بدل `id="email"` لحقل هاتف، `autoComplete="tel"`، وضغط المسافات ليظهر النموذج كاملاً على 1366×768.

## تنظيف

- **حُذف 249 سطر CSS ميت** من `globals.css` (557 → 308): `badge-*` الستّة، `card-hover`، `card-hover-v2`، `glass`، `glass-v2`، `glass-strong`، `hero-gradient-card`، `sidebar-item*`، `table-row-hover`، `pulse-live`، `stat-card-v2`، `stat-blob`، `text-gradient`، `border-glow-anim`، `animate-shine`، `glow-*` الخمسة، `.field`، و4 `@keyframes` بلا مستهلك.
- حُذف `settings/components/form-field.tsx` (استبدله `ui/field.tsx`).

## التحقّق

شُغّل الخادم الخلفي محلياً (`npm run dev:local`) وربطت حساب اختبار بملف مزوّد معتمد، وجرى الفحص في المتصفح على بيانات فعلية:

| الفحص | النتيجة |
|-------|---------|
| `npx tsc --noEmit` · `npm run lint` · `npm run build` · `npm test` | ✅ الأربعة نظيفة (8/8 اختبارات) |
| `<select>` أصلية متبقية | **0** |
| `toLocaleString` خارج `lib/format.ts` | متبقٍ في تلميحات ECharts فقط (مؤجّل للمرحلة 4) |
| أرقام هندية (٠-٩) في الصفحة | **0** بعد توحيد تاريخ الهيدر |
| القائمة المنسدلة الجديدة | تفتح بـ10 خيارات، خطّ عربي، صفّ 40px، تحديد مرئي، والتركيز يعود للزرّ بعد الاختيار ✅ |
| رقائق الفلاتر | اختيار "جارٍ التنفيذ" أنشأ رقاقة «الحالة: جارٍ التنفيذ» وأظهر «مسح الكل» ✅ |
| الخطأ داخل نموذج الدخول | ظهر «هذا الحساب ليس حساب مزود خدمة» داخل النموذج وبقي معروضاً ✅ |
| صفحة الخدمات ببيانات حقيقية | 9 بطاقات، فلترتان بـ`aria-label`، 0 زرّ أيقوني بلا تسمية، بلا تمرير أفقي ✅ |
| صفحة الدوام | 7 مفاتيح بتسميات عربية، الجمعة مغلقة، 12 منتقي وقت بنمط موحّد، `aria-live` لحالة الحفظ ✅ |
| `<Money>` | فجوة 4px بين الرقم والعملة، العملة بـ0.75em ولون ثانوي ✅ |
| أهداف اللمس | مفتاح التبديل 32×18 مرئياً لكن منطقته الفعلية **56×34** (مؤكَّد بـ`elementFromPoint`) — يتجاوز حد WCAG 2.2 AA (24×24) ✅ |

## ملاحظات صريحة

1. **تصحيح لتشخيصي في الخطة:** ادّعيت أن `toLocaleDateString("ar-SA")` في الهيدر يعرض **تقويماً هجرياً**. فحصته في المتصفح: يحلّ إلى `gregory` لا هجري. المشكلة الحقيقية كانت أسماء شهور خليجية (أغسطس) وأرقام هندية بينما بقية اللوحة شامية (آب) — وهي موحّدة الآن.
2. **قرار منتج اتّخذته نيابةً عنك:** نظام الأرقام صار **لاتينياً** (`123`) بدل الهندي (`١٢٣`). السبب: الأرقام اللاتينية هي السائدة في السياق التجاري السوري، وهي الوحيدة التي تصطفّ بعرض ثابت في الجداول. الحالة السابقة كانت ثلاثة أنظمة معاً على الشاشة نفسها، فأيّ توحيد كان سيغيّر شيئاً. **التبديل بسطر واحد**: غيّر `LOCALE` في `src/lib/format.ts`.
3. **`rounded-2xl` صار 20px بدل 16px** ومقاسات الأزرار تغيّرت قليلاً (`sm` 28→32px، `icon` 32→36px) لأهداف اللمس. تغييرات مقصودة.
4. **لم أستطع التحقّق من بطاقة الطلب وصفّ المعاملة ببيانات حقيقية**: نقطة `/orders` تُرجع صفراً رغم وجود 52 طلباً مرتبطاً بالمزوّد في قاعدة البيانات — مشكلة استعلام في الخادم الخلفي (على الأرجح فخّ `ObjectId` مقابل `string` المعروف في هذا المستودع)، **خارج نطاق عمل التصميم**. المكوّنان مُتحقَّقان بالأنواع والبناء فقط.
5. **لا سكرين‑شوت**: لوحة المتصفح غير معروضة في هذه البيئة. كل ما سبق مقيس من DOM والأنماط المحسوبة — أدقّ من العين لهذه البنود، لكنه لا يغني عن مراجعتك البصرية.
6. **مؤجّل عمداً للمرحلة 3**: الشريط الجانبي والهيدر ومزوّد الإشعارات الفورية — ما زالت تحمل ألوان tailwind خام و`premium-glass` و`toLocaleString` واحداً. وللمرحلة 4: ألوان الرسوم البيانية وتلميحاتها.

---

# نتائج المرحلة 3 — سجل التنفيذ

## ملفات جديدة

| الملف | الدور |
|-------|------|
| `src/lib/routes.ts` | **سجلّ المسارات الوحيد**: أيقونة + تسمية + عنوان + وصف لكل مسار، يغذّي الهيدر والشريط الجانبي معاً |
| `src/application/contexts/shell-context.tsx` | حالة القشرة: طيّ الشريط (محفوظ)، درج الجوال، والتحديث العام |
| `src/components/layout/notifications-menu.tsx` | مركز إشعارات حقيقي بجرس وعدّاد وسجلّ |
| `src/components/layout/availability-toggle.tsx` | مبدّل حالة التوفّر (متاح / مشغول / غير متاح) |

## الهيدر

- **العنوان صار في مكان واحد.** كان كل مسار يعرض `<h1>` مرّتين — في الهيدر الثابت وفي رأس مكتوب داخل الصفحة. الآن `<h1>` واحد (مُتحقَّق في المتصفح).
- حُذفت خريطة `pageTitles` المكرّرة بإيموجي 🏠📋🔧💰 واستُبدلت بأيقونات Lucide من `lib/routes.ts` نفسه الذي يغذّي الشريط الجانبي — فانتهى اختلاف التسميات بين الاثنين.
- **زرّ تحديث واحد يعمل فعلاً**: كان الهيدر يستقبل `onRefresh` ولا يمرّره التخطيط أبداً (زرّ ميت)، فصنعت الطلبات والمالية زرّيهما الخاصّين. الآن زرّ واحد يبطل صلاحية كل استعلامات react-query ويدور تلقائياً عبر `useIsFetching`، وحُذف الزرّان المكرّران.
- زرّ قائمة الجوال انتقل من زرّ عائم `z-[60]` كان يغطّي عنوان الصفحة إلى داخل الهيدر، مع `aria-expanded` و`aria-controls`.
- تاريخ الهيدر موحّد على `lib/format.ts` (كان `ar-SA` بأسماء شهور خليجية وأرقام هندية).

## الشريط الجانبي

- **طيّ/بسط على الشاشات الكبيرة** (264px ↔ 76px) مع حفظ التفضيل في `localStorage` عبر `useSyncExternalStore`، والمحتوى يتبع العرض بهامش منطقي.
- عند الطيّ: أيقونات فقط + `title` للتلميح + التسمية النصّية محفوظة كـ `sr-only` لقارئ الشاشة.
- **`aria-current="page"`** على الرابط النشط — كانت الحالة النشطة لوناً فقط لا يراه قارئ الشاشة.
- **مؤشّر اتصال صادق**: كانت شارة "LIVE" ثابتة مهما انقطع السوكِت. صارت تعكس `isConnected` الفعلي وتعرض «متصل» / «منقطع».
- **مبدّل حالة التوفّر** — عنصر التحكّم المفقود تماماً رغم أن `GET /providers/nearby` لا يُرجع إلا المزوّدين بحالة `online`، أي أن ظهور النشاط للعملاء كان يتوقّف على قيمة لا تعرضها اللوحة ولا تسمح بتغييرها. يستخدم `PUT /providers/me/status` الموجود مسبقاً.
- **درج الجوال صار حواراً حقيقياً**: `role="dialog"` + `aria-modal` + حبس تركيز (Tab يلتفّ في الاتجاهين) + إعادة التركيز إلى الزرّ عند الإغلاق + قفل تمرير الصفحة + Escape.

## التخطيط

- **عرض موحّد** `max-w-[1440px]` لكل المسارات — كانت تتفاوت بين بلا حدّ و`max-w-7xl` و`max-w-4xl` و`max-w-3xl`، فيقفز المحتوى عند كل تنقّل.
- **رابط "تخطّي إلى المحتوى"** أوّل عنصر قابل للتركيز، ومعالم `header`/`nav`/`main#main-content` صحيحة بتسميات مميّزة.
- سلّم الطبقات `--z-header/-sidebar/-scrim/-drawer/-overlay` مستعمل فعلياً بدل `z-30/50/55/58/60` العشوائية.
- **حُذف `overflow-x-hidden` و`max-w-[100vw]`** من `<main>`، وتأكّد عدم وجود تجاوز أفقي حقيقي بعدها.
- حُذف توكن `--sidebar-width` ووسيط الميديا المرتبط به (صار العرض من فئات على العنصر نفسه).

## مزوّد الإشعارات

- صار يحتفظ بسجلّ آخر 20 إشعاراً مع حالة قراءة، ويكشف `isConnected` — فالجرس والمؤشّر يقرآن من مصدر واحد بلا فتح اتصال سوكِت ثانٍ.
- الجرس كان `notifCount = 0` خلف `// TODO`؛ صار قائمة منسدلة بعدّاد وحالة فراغ ومسح.
- حوار الطلب الوارد: أزيلت المحاذاة المعكوسة يدوياً (`justify-end` + الأيقونة بعد النص في كل صفّ) لأن المستند عربي الاتجاه أصلاً، ورُتّبت الأزرار على قاعدة «التراجع أولاً، التأكيد أخيراً».

## التحقّق

`tsc` ✅ · `eslint` ✅ · `build` ✅ · `vitest` 8/8 ✅ — والفحوص التالية على **بناء الإنتاج** مع خادم خلفي حيّ:

| الفحص | النتيجة |
|-------|---------|
| عدد `<h1>` في الصفحة | **1** (كان 2) ✅ |
| رابط التخطّي أوّل عنصر | ✅ |
| المعالم | `header` + `main#main-content` + معلمَا تنقّل بتسميتين مختلفتين ✅ |
| `aria-current="page"` | موجود على الرابط النشط ✅ |
| طيّ الشريط | 264px ↔ 76px، والهامش يتبعه، والتفضيل ينجو من إعادة تشغيل الخادم ✅ |
| الشريط المطويّ | `title` للتلميح + التسمية `sr-only` ✅ |
| درج الجوال (390px) | مغلق `translate:100%` → مفتوح `translate:0`، الصندوق 110→390 وقابل للنقر فعلياً من x=125 إلى x=385 ✅ |
| حبس التركيز | Tab من الأخير يلتفّ للأوّل، Shift+Tab من الأوّل يلتفّ للأخير ✅ |
| Escape | يغلق، يفكّ قفل التمرير، ويعيد التركيز إلى زرّ الفتح ✅ |
| مؤشّر الاتصال | عرض «متصل» بعد اتصال السوكِت فعلاً بالخادم ✅ |
| مبدّل التوفّر | اختيار «مشغول» ⇒ `GET /providers/me` يُرجع `status:"busy"` ✅ |
| مركز الإشعارات | ينفتح داخل حدود الشاشة بحالة فراغ صحيحة ✅ |
| التجاوز الأفقي بعد حذف `overflow-x-hidden` | `scrollWidth == clientWidth` ✅ |

## ملاحظات صريحة

1. **خطأ حقيقي اكتُشف وأُصلح أثناء التحقّق:** الدرج كان يعتمد على التبديل بين `translate-x-full` و`translate-x-0`. في Tailwind v4 تكتب الأداتان خاصية `translate` بصيغتين مختلفتين (نسبة حرفية مقابل متغيّر)، فبقيت الإزاحة القديمة فاعلة وظلّ الدرج **خارج الشاشة رغم فتحه** — مؤكَّد بـ`elementFromPoint` الذي لم يصله في أي نقطة. استُبدل بقاعدة `.nav-drawer[data-open="true"]` صريحة في `globals.css`.
2. **خادم التطوير ضلّلني أربع مرات**: Tailwind في وضع `--webpack` لا يولّد إلا أصناف الحالات التي رُسمت فعلاً، ووصلة HMR كانت تفشل — فظهرت `w-[76px]`/`w-[264px]`/`w-[280px]` وكأنها لا تعمل. تحقّقت من وجود القواعد الأربع في CSS الإنتاج ثم أعدت الفحص كلّه على `next start`. **خلاصة عملية: افحص هذه الأنماط على بناء الإنتاج، لا على dev.**
3. **`PageHeader` تحوّل إلى `PageToolbar`** (بلا عنوان). هذا تراجع مقصود عن أحد مخرجات المرحلة 2: حلّ معماريّة المعلومات في هذه المرحلة أظهر أن العنوان يجب أن يملكه الهيدر وحده.
4. **ثغرة موجودة سلفاً لاحظتها ولم أعالجها** (خارج نطاق التصميم): الكوكي `provider_access_token` غير مقيّد بالمنفذ بينما الجلسة في `localStorage` مقيّدة به، فقد يمرّر الوسيط الطلب بينما يرى العميل نفسه غير مسجَّل الدخول — فتظهر صفحة فارغة.
5. **لا سكرين‑شوت**: لوحة المتصفح لا تُصوّر في هذه البيئة؛ كل ما سبق مقيس من DOM والأنماط المحسوبة و`elementFromPoint`.
